/**
 * {{PascalName}} view — served by this plugin's own Vite dev server and
 * loaded in an iframe by the orchestrator.
 *
 * To talk to the main process: import `connectRpc` from `#zenbu/zenrpc` and
 * `connectReplica` from `#zenbu/kyju/transport`, mirroring
 * `packages/init/src/renderer/lib/ws-connection.ts`.
 */
export function App() {
  return (
    <div style={{ padding: 20, fontFamily: "system-ui" }}>
      <h1>{{name}}</h1>
      <p>Your view is live. Edit <code>src/view/App.tsx</code> to iterate.</p>
    </div>
  )
}
