/**
 * @acp/context — ACP (Agent Client Protocol) documentation, schema, and examples.
 *
 * This package vendors the official ACP specification from:
 *   https://github.com/agentclientprotocol/agent-client-protocol
 *
 * And examples from the TypeScript SDK:
 *   https://github.com/agentclientprotocol/typescript-sdk
 *
 * Directory layout:
 *
 *   docs/                         — Full ACP documentation (mdx/md)
 *     get-started/                — Introduction, architecture, agents, clients
 *     protocol/                   — Protocol spec: init, sessions, prompts, tool calls, etc.
 *       draft/                    — Draft protocol additions (cancellation, unstable schema)
 *     libraries/                  — SDK docs (TypeScript, Python, Rust, Java, Kotlin)
 *     community/                  — Governance, contributing, code of conduct
 *     rfds/                       — Request For Discussion proposals
 *
 *   schema/                       — JSON schema files
 *     schema.json                 — Stable ACP schema
 *     schema.unstable.json        — Unstable/draft ACP schema
 *     meta.json                   — Schema metadata
 *     meta.unstable.json          — Unstable schema metadata
 *
 *   examples/                     — TypeScript SDK examples (from agentclientprotocol/typescript-sdk)
 *     agent.ts                    — Example agent implementation
 *     client.ts                   — Example client implementation
 */

export {};
